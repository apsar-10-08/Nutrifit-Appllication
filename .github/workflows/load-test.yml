name: Load Tests

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  run-load-tests:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          
      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install openpyxl
          
      - name: Generate Realistic Load Tests
        run: python generate_realistic_load_tests.py
        
      - name: Upload Load Test Report Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Load_Test_Report
          path: testing-reports/Load_Test_Report.xlsx
          retention-days: 90
          if-no-files-found: error
