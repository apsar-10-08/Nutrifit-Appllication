name: Selenium Mock Tests

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  run-mock-selenium-tests:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
        
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'
          
      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install openpyxl
          
      - name: Generate 400 Mock Selenium Tests
        run: python mock_selenium_tests.py
        
      - name: Upload Selenium Mock Test Report Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Selenium_Mock_Test_Report
          path: testing-reports/Selenium_Mock_Test_Report.xlsx
          retention-days: 90
          if-no-files-found: error
