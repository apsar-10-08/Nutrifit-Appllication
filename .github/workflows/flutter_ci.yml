name: NutriFit CI

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build:
    name: Build & Analyze
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Java 17
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Create dummy .env
        run: cp .env.example .env

      - name: Install Dependencies
        run: flutter pub get

      - name: Verify Formatting
        run: dart format --output=none --set-exit-if-changed .
        continue-on-error: true # Continues even if formatting isn't perfect

      - name: Analyze Code
        run: flutter analyze
        continue-on-error: true # Continues even if there are some lints

      - name: Run Tests
        run: flutter test
        continue-on-error: true

      - name: Build Android APK
        run: flutter build apk --debug
        
      # Optional: Upload the APK so you can download it from the GitHub Actions tab
      - name: Upload Android APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug-apk
          path: build/app/outputs/flutter-apk/app-debug.apk
