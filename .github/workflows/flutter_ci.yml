name: Flutter CI

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-android:
    name: Build Android APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Create .env file
        run: |
          echo "SUPABASE_URL=${{ secrets.SUPABASE_URL }}" > .env
          echo "SUPABASE_ANON_KEY=${{ secrets.SUPABASE_ANON_KEY }}" >> .env

      - name: Get dependencies
        run: flutter pub get

      - name: Analyze project
        run: flutter analyze || true

      - name: Build Android APK
        run: flutter build apk --debug

      - name: Upload Android APK
        uses: actions/upload-artifact@v4
        with:
          name: NutriFit-debug-apk
          path: build/app/outputs/flutter-apk/app-debug.apk

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install Python Dependencies
        run: pip install openpyxl

      - name: Update Test Report Stats
        run: python appium_test_reports/update.py || true

      - name: Generate Appium Test Report
        run: python appium_test_reports/convert_to_excel.py || true

      - name: Upload Appium Test Report
        uses: actions/upload-artifact@v4
        with:
          name: appium-test-report
          path: appium_test_reports/*.xlsx
